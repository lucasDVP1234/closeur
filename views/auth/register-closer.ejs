<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Candidature Closer - CLOSER</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@200;300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <script>
    tailwind.config = {
        theme: {
            extend: {
                fontFamily: {
                    display: ['Outfit', 'sans-serif'],
                    mono: ['JetBrains Mono', 'monospace'],
                },
                colors: {
                    surface: { 50: '#fafafa', 100: '#f4f4f5', 200: '#e4e4e7', 300: '#d4d4d8', 800: '#111113', 900: '#09090b', 950: '#050506' },
                    accent: { 400: '#a78bfa', 500: '#8b5cf6', 600: '#7c3aed', 700: '#6d28d9' },
                }
            }
        }
    }
    </script>
    <style>
        *, *::before, *::after { box-sizing: border-box; }
        body { font-family: 'Outfit', sans-serif; background: #050506; color: #fafafa; overflow-x: hidden; position: relative; }

        body::after {
            content: '';
            position: fixed; inset: -20%;
            z-index: 0;
            pointer-events: none;
            background:
                radial-gradient(800px 450px at 25% 15%, rgba(139, 92, 246, 0.16), transparent 60%),
                radial-gradient(600px 380px at 80% 75%, rgba(124, 58, 237, 0.10), transparent 65%);
            opacity: 0.9;
        }
        body > * { position: relative; z-index: 1; }

        body::before {
            content: '';
            position: fixed; inset: 0; z-index: 9999;
            pointer-events: none;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
            opacity: 0.5;
        }

        ::selection { background: rgba(139,92,246,0.4); color: white; }

        ::-webkit-scrollbar { width: 5px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: rgba(139,92,246,0.3); border-radius: 99px; }
        ::-webkit-scrollbar-thumb:hover { background: rgba(139,92,246,0.6); }

        .auth-card {
            background: linear-gradient(180deg, rgba(255,255,255,0.05), rgba(255,255,255,0.015));
            border: 1px solid rgba(255,255,255,0.08);
            box-shadow:
                0 40px 100px -50px rgba(0,0,0,0.8),
                0 0 0 1px rgba(255,255,255,0.02) inset,
                0 0 120px -40px rgba(139,92,246,0.06);
        }

        .auth-input {
            background: rgba(255,255,255,0.04);
            border: 1px solid rgba(255,255,255,0.10);
            transition: all 0.3s ease;
        }
        .auth-input:hover { border-color: rgba(139, 92, 246, 0.25); }
        .auth-input:focus {
            border-color: rgba(139, 92, 246, 0.7);
            box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.12);
            background: rgba(255,255,255,0.06);
            outline: none;
        }

        .step-content {
            display: none;
            opacity: 0;
            transform: translateY(12px);
            transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .step-content.active {
            display: block;
            opacity: 1;
            transform: translateY(0);
        }

        .selection-card {
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.08);
            transition: all 0.25s ease;
        }
        .selection-card:hover {
            background: rgba(255,255,255,0.06);
            border-color: rgba(255,255,255,0.14);
        }
        .selection-card:has(input:checked) {
            background: rgba(139, 92, 246, 0.12);
            border-color: rgba(139, 92, 246, 0.5);
            box-shadow: 0 0 20px rgba(139, 92, 246, 0.12);
        }

        .progress-step {
            width: 100%;
            height: 3px;
            background: rgba(255,255,255,0.06);
            border-radius: 99px;
            overflow: hidden;
            position: relative;
        }
        .progress-step .fill {
            height: 100%;
            background: linear-gradient(90deg, #8b5cf6, #a78bfa);
            border-radius: 99px;
            transition: width 0.5s cubic-bezier(0.16, 1, 0.3, 1);
            box-shadow: 0 0 12px rgba(139,92,246,0.5);
        }

        .btn-next {
            background: linear-gradient(135deg, #8b5cf6, #7c3aed);
            transition: all 0.3s ease;
            position: relative; overflow: hidden;
        }
        .btn-next::before {
            content: ''; position: absolute; inset: 0;
            background: linear-gradient(135deg, rgba(255,255,255,0.15), transparent 60%);
            opacity: 0; transition: opacity 0.3s;
        }
        .btn-next:hover::before { opacity: 1; }
        .btn-next:hover { box-shadow: 0 16px 40px -12px rgba(139,92,246,0.4); transform: translateY(-1px); }

        .btn-back {
            color: rgba(255,255,255,0.4);
            transition: all 0.2s ease;
        }
        .btn-back:hover { color: rgba(255,255,255,0.7); }

        .btn-submit {
            background: linear-gradient(135deg, #8b5cf6, #6d28d9);
            box-shadow: 0 0 30px rgba(139,92,246,0.25);
            transition: all 0.3s ease;
        }
        .btn-submit:hover {
            box-shadow: 0 20px 50px -15px rgba(139,92,246,0.5);
            transform: translateY(-1px);
        }

        .checkbox-list {
            max-height: 260px;
            overflow-y: auto;
        }

        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(16px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-up { animation: fadeUp 0.5s ease-out forwards; }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-4 sm:p-6">

    <div class="w-full max-w-2xl">

        <!-- Logo -->
        <div class="text-center mb-6 animate-fade-up">
            <a href="/" class="inline-flex items-center gap-2.5 group">
                <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-accent-500 to-accent-700 flex items-center justify-center shadow-lg shadow-accent-500/20">
                    <span class="text-white font-extrabold text-xs tracking-tight">C.</span>
                </div>
                <span class="text-white/60 text-sm font-medium tracking-wide group-hover:text-white/80 transition-colors">sefairecloser.com</span>
            </a>
        </div>

        <!-- Card -->
        <div class="auth-card rounded-2xl p-6 sm:p-8 animate-fade-up">

            <% if (locals.error) { %>
                <div class="mb-6 bg-red-500/10 border border-red-500/20 text-red-400 px-4 py-3 rounded-xl text-xs font-bold uppercase tracking-wide flex items-center gap-2 animate-pulse">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 shrink-0" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
                    </svg>
                    <%= error %>
                </div>
            <% } %>

            <!-- Header + Progress -->
            <div class="mb-7">
                <div class="flex justify-between items-end mb-4">
                    <div>
                        <h1 class="text-xl sm:text-2xl font-bold text-white tracking-tight">
                            Candidature <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent-400 to-accent-600">Elite</span>
                        </h1>
                        <p class="text-white/30 text-xs mt-1 font-mono">
                            Étape <span id="current-step-num" class="text-accent-400">1</span> / 5
                        </p>
                    </div>
                    <a href="/login" class="text-[11px] text-white/30 hover:text-white/60 transition-colors font-medium">Déjà un compte ?</a>
                </div>
                <div class="progress-step">
                    <div id="progress-bar" class="fill" style="width: 20%"></div>
                </div>
            </div>

            <!-- Multi-step Form -->
            <form id="multi-step-form" action="/register/closer" method="POST" enctype="multipart/form-data">

                <!-- Step 1: Profil -->
                <div class="step-content active" data-step="1">
                    <h3 class="text-lg font-semibold mb-5 text-white/90">Votre Profil</h3>
                    
                    <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2.5 block">Vous êtes plutôt :</label>
                    <div class="grid grid-cols-3 gap-3 mb-6">
                        <label class="selection-card cursor-pointer rounded-xl p-4 text-center">
                            <input type="radio" name="profileType" value="Closeur" checked class="hidden">
                            <div class="text-2xl mb-1.5">🎯</div>
                            <span class="text-sm font-semibold text-white/80">Closeur</span>
                        </label>
                        <label class="selection-card cursor-pointer rounded-xl p-4 text-center">
                            <input type="radio" name="profileType" value="Commercial" class="hidden">
                            <div class="text-2xl mb-1.5">💼</div>
                            <span class="text-sm font-semibold text-white/80">Commercial</span>
                        </label>
                        <label class="selection-card cursor-pointer rounded-xl p-4 text-center">
                            <input type="radio" name="profileType" value="Setter" class="hidden">
                            <div class="text-2xl mb-1.5">📞</div>
                            <span class="text-sm font-semibold text-white/80">Setter</span>
                        </label>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Marché</label>
                            <select name="market" class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm">
                                <option value="B2C">B2C (Particuliers)</option>
                                <option value="B2B">B2B (Entreprises)</option>
                                <option value="Les deux">Les deux</option>
                            </select>
                        </div>
                        <div>
                            <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Disponibilité</label>
                            <select name="availability" class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm">
                                <option value="Full Time">Full Time</option>
                                <option value="Mi-temps">Mi-temps</option>
                            </select>
                        </div>
                    </div>
                </div>
                
                <!-- Step 2: Identité -->
                <div class="step-content" data-step="2">
                    <h3 class="text-lg font-semibold mb-5 text-white/90">Identité & Accès</h3>
                    <div class="grid grid-cols-2 gap-4 mb-4">
                        <input type="text" name="prenom" placeholder="Prénom" required class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20">
                        <input type="text" name="nom" placeholder="Nom" required class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20">
                    </div>
                    <div class="space-y-4">
                        <input type="email" name="email" placeholder="Email Pro" required class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20">
                        <input type="tel" name="telephone" placeholder="Téléphone / WhatsApp" required class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20">
                        <input type="password" name="password" placeholder="Mot de passe" required class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20">
                    </div>
                </div>

                <!-- Step 3: Expérience -->
                <div class="step-content" data-step="3">
                    <h3 class="text-lg font-semibold mb-5 text-white/90">Expérience & Track Record</h3>

                    <div class="grid grid-cols-2 gap-4 mb-5">
                        <div>
                            <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Années d'expérience</label>
                            <input type="number" name="yearsExperience" placeholder="Ex: 3" class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20">
                        </div>
                        <div>
                            <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">CA Généré (€)</label>
                            <input type="number" name="totalClosed" placeholder="Ex: 500000" class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm font-mono placeholder-white/20">
                        </div>
                    </div>

                    <div class="mb-5">
                        <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Types de produits closés</label>
                        <div class="auth-input rounded-xl p-4 checkbox-list">
                            <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Crypto_Trading" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">Cryptomonnaies & Trading</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Ecommerce_Dropshipping" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">E-commerce & Dropshipping</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Marketing_SMMA" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">Marketing Digital & SMMA</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Creation_contenu" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">CM & Création de contenu</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Coaching_Business" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">Coaching Business</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="IA" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">Intelligence Artificielle</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="SaaS_B2B" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">SaaS & Logiciels B2B</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Fitness" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">Perte de poids & Fitness</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Dev_Perso" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">Développement personnel</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Love_Coaching" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">Relations & Love Coaching</span>
                                </label>
                                <label class="flex items-center gap-3 cursor-pointer group hover:bg-white/4 p-2 rounded-lg transition-colors">
                                    <input type="checkbox" name="productTypes" value="Energies" class="w-3.5 h-3.5 accent-[#8b5cf6] rounded"> 
                                    <span class="text-sm text-white/60 group-hover:text-white/80">Énergies Renouvelables</span>
                                </label>
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Partenaires passés</label>
                        <textarea name="pastClients" rows="2" placeholder="Ex: Google, TheFamily, Oussama Ammar..." class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20 resize-none"></textarea>
                    </div>
                </div>

                <!-- Step 4: Conditions -->
                <div class="step-content" data-step="4">
                    <h3 class="text-lg font-semibold mb-5 text-white/90">Vos conditions</h3>

                    <div class="mb-5">
                        <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2.5 block">Type de contrat recherché</label>
                        <div class="grid grid-cols-1 gap-2.5">
                            <label class="selection-card cursor-pointer flex items-center gap-3 rounded-xl p-3.5">
                                <input type="checkbox" name="contractType" value="100% Commission" class="hidden">
                                <div class="w-4 h-4 rounded-full border border-white/20 bg-transparent shrink-0"></div>
                                <span class="text-sm text-white/70">100% Commission (High Ticket)</span>
                            </label>
                            <label class="selection-card cursor-pointer flex items-center gap-3 rounded-xl p-3.5">
                                <input type="checkbox" name="contractType" value="Fixe + Commission" class="hidden">
                                <div class="w-4 h-4 rounded-full border border-white/20 bg-transparent shrink-0"></div>
                                <span class="text-sm text-white/70">Fixe + Commission</span>
                            </label>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Rémunération / Mois (€)</label>
                            <input type="number" name="desiredIncome" placeholder="Ex: 5000" class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm font-mono placeholder-white/20">
                        </div>
                        <div>
                            <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Type de mission</label>
                            <select name="missionType" class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm">
                                <option value="Long terme">Long terme</option>
                                <option value="Mission (Webi/Challenge)">Mission (Webi/Challenge)</option>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- Step 5: Finalisation -->
                <div class="step-content" data-step="5">
                    <h3 class="text-lg font-semibold mb-5 text-white/90">Le mot de la fin</h3>

                    <div class="mb-5">
                        <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Un bon closeur pour vous c'est...</label>
                        <textarea name="closingPhilosophy" required rows="3" placeholder="Votre définition..." class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20 resize-none"></textarea>
                    </div>

                    <div class="mb-5">
                        <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Photo de profil</label>
                        <div class="auth-input border-dashed rounded-xl p-6 text-center cursor-pointer relative group hover:border-accent-500/40">
                            <input type="file" name="photo" accept="image/*" required class="absolute inset-0 opacity-0 cursor-pointer z-10" onchange="previewFile(this)">
                            <div id="preview-container" class="transition-transform duration-300 group-hover:scale-105">
                                <div class="w-12 h-12 rounded-full bg-accent-500/10 flex items-center justify-center mx-auto mb-2">
                                    <i class="fas fa-camera text-accent-400 text-lg"></i>
                                </div>
                                <p class="text-accent-400 font-medium text-sm">Uploader ma photo</p>
                                <p class="text-white/20 text-xs mt-1">JPG, PNG — max 5MB</p>
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="text-[11px] text-white/35 uppercase font-semibold tracking-wider mb-2 block">Lien Démo / Vidéo de Closing</label>
                        <input type="url" name="videoUrl" placeholder="https://loom.com/..." class="auth-input w-full rounded-xl px-4 py-3.5 text-white text-sm placeholder-white/20">
                    </div>
                </div>

                <!-- Navigation buttons -->
                <div class="flex justify-between mt-7 pt-5 border-t border-white/6">
                    <button type="button" id="prev-btn" class="btn-back hidden px-5 py-3 rounded-xl font-medium text-sm">
                        <i class="fas fa-arrow-left mr-1.5 text-[10px]"></i> Retour
                    </button>
                    <button type="button" id="next-btn" class="btn-next text-white px-7 py-3 rounded-xl font-semibold text-sm ml-auto">
                        Suivant <i class="fas fa-arrow-right ml-1.5 text-[10px]"></i>
                    </button>
                    <button type="submit" id="submit-btn" class="btn-submit hidden text-white px-7 py-3 rounded-xl font-bold text-sm tracking-wide ml-auto">
                        VALIDER MA FICHE
                    </button>
                </div>

            </form>
        </div>

        <!-- Bottom -->
        <p class="text-center mt-5 text-[11px] text-white/15 tracking-wider">CLOSER. — Plateforme Elite</p>
    </div>

    <script>
        let currentStep = 1;
        const totalSteps = 5;

        const nextBtn = document.getElementById('next-btn');
        const prevBtn = document.getElementById('prev-btn');
        const submitBtn = document.getElementById('submit-btn');
        const progressBar = document.getElementById('progress-bar');
        const stepNum = document.getElementById('current-step-num');

        function updateUI() {
            document.querySelectorAll('.step-content').forEach(el => el.classList.remove('active'));
            document.querySelector(`.step-content[data-step="${currentStep}"]`).classList.add('active');

            if (currentStep === 1) prevBtn.classList.add('hidden');
            else prevBtn.classList.remove('hidden');

            if (currentStep === totalSteps) {
                nextBtn.classList.add('hidden');
                submitBtn.classList.remove('hidden');
            } else {
                nextBtn.classList.remove('hidden');
                submitBtn.classList.add('hidden');
            }

            const percentage = (currentStep / totalSteps) * 100;
            progressBar.style.width = percentage + '%';
            stepNum.innerText = currentStep;
        }

        nextBtn.addEventListener('click', () => {
            const currentSlide = document.querySelector(`.step-content[data-step="${currentStep}"]`);
            const inputs = currentSlide.querySelectorAll('input[required], textarea[required], select[required]');
            let isValid = true;

            const checkboxes = currentSlide.querySelectorAll('input[type="checkbox"]');
            if (checkboxes.length > 0) {
                const isAnyChecked = Array.from(checkboxes).some(cb => cb.checked);
                if (!isAnyChecked) {
                    isValid = false;
                    alert("Veuillez sélectionner au moins une option."); 
                }
            }
            
            inputs.forEach(input => {
                if (!input.value) {
                    isValid = false;
                    input.style.borderColor = 'rgba(239,68,68,0.6)';
                    setTimeout(() => input.style.borderColor = '', 2000);
                }
            });

            if (isValid && currentStep < totalSteps) {
                currentStep++;
                updateUI();
            }
        });

        prevBtn.addEventListener('click', () => {
            if (currentStep > 1) {
                currentStep--;
                updateUI();
            }
        });

        function previewFile(input) {
            const file = input.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    document.getElementById('preview-container').innerHTML = `
                        <img src="${e.target.result}" class="w-16 h-16 rounded-full object-cover mx-auto border-2 border-accent-500 shadow-lg shadow-accent-500/20">
                        <p class="text-green-400 text-xs mt-2 font-medium">Photo chargée</p>
                    `;
                }
                reader.readAsDataURL(file);
            }
        }
    </script>
</body>
</html>