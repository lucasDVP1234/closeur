<!DOCTYPE html>
<html lang="fr" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THE CLOSERS | Elite Directory</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;500;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { font-family: 'Space Grotesk', sans-serif; background-color: #050505; color: #ffffff; }
        .glass-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        .neon-border:hover {
            border-color: rgba(139, 92, 246, 0.5);
            box-shadow: 0 0 20px rgba(139, 92, 246, 0.2);
        }
        /* Custom scrollbar pour les filtres si besoin */
        input[type="number"]::-webkit-inner-spin-button,
        input[type="number"]::-webkit-outer-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }
    </style>
</head>
<body class="min-h-screen bg-[url('https://grainy-gradients.vercel.app/noise.svg')]">

    <nav class="border-b border-white/10 py-5 sticky top-0 z-50 bg-[#050505]/80 backdrop-blur-xl transition-all duration-300">
        <div class="max-w-7xl mx-auto px-6 flex justify-between items-center">
            <a href="/" class="group flex items-center gap-3">
                <div class="w-1 h-5 bg-purple-600 rounded-full shadow-[0_0_10px_rgba(147,51,234,0.8)] group-hover:h-8 group-hover:bg-white transition-all duration-300"></div>
                <h1 class="text-xl font-bold tracking-widest text-white">THE <span class="text-purple-500 group-hover:text-white transition-colors">CLOSERS</span>.</h1>
            </a>
            
            <div class="flex items-center gap-6">
                <% if (user) { %>
                    <div class="flex items-center gap-4 bg-white/5 border border-white/10 px-1 py-1 pr-4 rounded-full backdrop-blur-md">
                        <% if (user.role === 'closer') { %>
                            <a href="/dashboard" class="flex items-center gap-2 bg-white/10 hover:bg-purple-600 hover:text-white text-white px-4 py-2 rounded-full transition-all text-xs font-bold uppercase tracking-wide group">
                                <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse group-hover:bg-white"></span>
                                Mon Cockpit
                            </a>
                        <% } else { %>
                            <div class="flex items-center gap-2 px-4 py-2">
                                <span class="text-lg">🏢</span>
                                <span class="text-sm font-bold text-gray-200"><%= user.name %></span>
                            </div>
                        <% } %>
                        <div class="w-px h-4 bg-white/10"></div>
                        <a href="/logout" class="text-red-400 hover:text-red-300 hover:bg-red-500/10 p-2 rounded-full transition-all" title="Déconnexion">
                            <i class="fas fa-power-off text-sm"></i>
                        </a>
                    </div>
                <% } else { %>
                    <div class="flex items-center gap-6">
                        <a href="/login" class="text-gray-400 hover:text-white text-sm font-bold transition-colors">Connexion</a>
                        <a href="/register-choice" class="group bg-white text-black px-6 py-2.5 rounded-full font-bold hover:bg-purple-600 hover:text-white transition-all duration-300 text-xs uppercase tracking-wider shadow-[0_0_15px_rgba(255,255,255,0.1)] hover:shadow-[0_0_20px_rgba(147,51,234,0.5)] flex items-center gap-2">
                            Rejoindre l'élite 
                            <span class="hidden group-hover:inline-block transform translate-x-0 group-hover:translate-x-1 transition-transform">→</span>
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </nav>

    <header class="py-16 px-6 max-w-7xl mx-auto">
        <h2 class="text-5xl md:text-7xl font-bold mb-8 text-transparent bg-clip-text bg-gradient-to-r from-white to-gray-600 tracking-tighter">
            Trouvez votre <br> arme fatale.
        </h2>
        
        <form action="/" method="GET" class="glass-card p-6 md:p-8 rounded-3xl mt-8 shadow-2xl relative overflow-hidden">
            <div class="absolute top-0 right-0 w-64 h-64 bg-purple-900/20 rounded-full blur-3xl -mr-20 -mt-20 pointer-events-none"></div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 relative z-10">
                
                <div class="lg:col-span-2 space-y-2">
                    <label class="text-[10px] uppercase font-bold text-gray-500 tracking-wider ml-1">Mots-clés</label>
                    <div class="relative">
                        <i class="fas fa-search absolute left-4 top-1/2 -translate-y-1/2 text-gray-500"></i>
                        <input type="text" name="skill" placeholder="Ex: Immo, High Ticket, B2B..." value="<%= filters.skill || '' %>" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 pl-10 pr-4 text-white focus:border-purple-500 outline-none transition-all placeholder-gray-600">
                    </div>
                </div>

                <div class="space-y-2">
                    <label class="text-[10px] uppercase font-bold text-gray-500 tracking-wider ml-1">Niveau</label>
                    <div class="relative">
                        <select name="experience" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 px-4 text-white focus:border-purple-500 outline-none appearance-none cursor-pointer">
                            <option value="">Tout niveau</option>
                            <option value="Junior" <%= filters.experience === 'Junior' ? 'selected' : '' %>>Junior</option>
                            <option value="Confirmé" <%= filters.experience === 'Confirmé' ? 'selected' : '' %>>Confirmé</option>
                            <option value="Expert" <%= filters.experience === 'Expert' ? 'selected' : '' %>>Expert</option>
                            <option value="Killer" <%= filters.experience === 'Killer' ? 'selected' : '' %>>Killer</option>
                        </select>
                        <i class="fas fa-chevron-down absolute right-4 top-1/2 -translate-y-1/2 text-gray-500 pointer-events-none text-xs"></i>
                    </div>
                </div>

                <div class="space-y-2">
                    <label class="text-[10px] uppercase font-bold text-gray-500 tracking-wider ml-1">Trier par</label>
                    <div class="relative">
                        <select name="sort" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 px-4 text-white focus:border-purple-500 outline-none appearance-none cursor-pointer">
                            <option value="new" <%= filters.sort === 'new' ? 'selected' : '' %>>Les plus récents</option>
                            <option value="best" <%= filters.sort === 'best' ? 'selected' : '' %>>Top Performers (€)</option>
                        </select>
                        <i class="fas fa-sort absolute right-4 top-1/2 -translate-y-1/2 text-gray-500 pointer-events-none text-xs"></i>
                    </div>
                </div>

                <div class="space-y-2">
                    <label class="text-[10px] uppercase font-bold text-gray-500 tracking-wider ml-1">Âge</label>
                    <div class="flex gap-2">
                        <input type="number" name="ageMin" placeholder="Min" value="<%= filters.ageMin || '' %>" class="w-1/2 bg-black/40 border border-white/10 rounded-xl py-3 px-4 text-white focus:border-purple-500 outline-none">
                        <input type="number" name="ageMax" placeholder="Max" value="<%= filters.ageMax || '' %>" class="w-1/2 bg-black/40 border border-white/10 rounded-xl py-3 px-4 text-white focus:border-purple-500 outline-none">
                    </div>
                </div>

                <div class="lg:col-span-2 space-y-2">
                    <label class="text-[10px] uppercase font-bold text-gray-500 tracking-wider ml-1">Revenu minimum closé</label>
                    <div class="relative">
                        <span class="absolute left-4 top-1/2 -translate-y-1/2 text-gray-500">€</span>
                        <input type="number" name="revenueMin" placeholder="Ex: 100000" value="<%= filters.revenueMin || '' %>" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 pl-8 pr-4 text-white focus:border-purple-500 outline-none font-mono">
                    </div>
                </div>

                <div class="flex items-end">
                    <button type="submit" class="w-full bg-white text-black hover:bg-purple-600 hover:text-white py-3 rounded-xl font-bold transition-all shadow-[0_0_20px_rgba(255,255,255,0.1)] flex justify-center items-center gap-2 uppercase tracking-wide">
                        <i class="fas fa-filter text-sm"></i> Filtrer les profils
                    </button>
                </div>
            </div>
        </form>
    </header>

    <main class="max-w-7xl mx-auto px-6 pb-20">
        
        <div class="flex justify-between items-center mb-6">
            <p class="text-gray-400 text-sm"><span class="text-white font-bold"><%= closers.length %></span> talents trouvés</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% closers.forEach(closer => { %>
                <div class="glass-card rounded-3xl overflow-hidden group neon-border transition-all duration-300 relative flex flex-col h-full">
                    
                    <div class="h-72 w-full relative overflow-hidden">
                        <img src="<%= closer.photoUrl %>" alt="<%= closer.nom %>" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 grayscale group-hover:grayscale-0">
                        <div class="absolute bottom-0 left-0 w-full bg-gradient-to-t from-[#050505] to-transparent h-24"></div>
                        
                        <span class="absolute top-4 right-4 bg-black/80 backdrop-blur border border-white/20 px-3 py-1 text-[10px] font-bold uppercase tracking-wider rounded-full shadow-lg">
                            <%= closer.experienceLevel %>
                        </span>
                    </div>

                    <div class="p-6 pt-2 flex flex-col flex-1 relative">
                        <div class="flex justify-between items-start mb-4">
                            <div>
                                <h3 class="text-2xl font-bold text-white leading-none mb-1"><%= closer.prenom %> <%= closer.nom %></h3>
                                <p class="text-gray-500 text-xs font-mono"><%= closer.age %> ans</p>
                            </div>
                        </div>

                        <div class="grid grid-cols-2 gap-3 mb-6">
                            <div class="bg-white/5 rounded-lg p-3 border border-white/5">
                                <p class="text-[10px] text-gray-500 uppercase tracking-wide mb-1">Total Closé</p>
                                <p class="text-purple-400 font-mono font-bold text-lg"><%= closer.totalClosed.toLocaleString() %>€</p>
                            </div>
                            <div class="bg-white/5 rounded-lg p-3 border border-white/5">
                                <p class="text-[10px] text-gray-500 uppercase tracking-wide mb-1">Challenge</p>
                                <p class="text-white text-xs truncate font-medium"><%= closer.lastChallenge || '-' %></p>
                            </div>
                        </div>

                        <div class="flex flex-wrap gap-2 mb-6">
                            <% closer.skills.slice(0, 3).forEach(skill => { %>
                                <span class="px-2 py-1 bg-white/5 border border-white/10 rounded text-[9px] uppercase font-bold text-gray-300 tracking-wide">
                                    <%= skill %>
                                </span>
                            <% }) %>
                            <% if(closer.skills.length > 3) { %>
                                <span class="px-2 py-1 bg-white/5 rounded text-[9px] text-gray-500">+<%= closer.skills.length - 3 %></span>
                            <% } %>
                        </div>

                        <div class="mt-auto space-y-3">
                            <div class="flex items-start gap-2 text-xs text-gray-500 italic border-l-2 border-purple-500/30 pl-3">
                                <i class="fas fa-quote-left text-[8px] mt-1"></i>
                                <%= closer.misc || 'Prêt à closer.' %>
                            </div>

                            <div class="grid grid-cols-2 gap-3 mt-4 pt-4 border-t border-white/5">
                                
                                <% if (user) { %>
                                    <div onclick="revealContact(this)" class="cursor-pointer bg-white text-black py-3 rounded-xl font-bold text-center hover:bg-gray-200 transition text-xs uppercase tracking-wide flex items-center justify-center gap-2 overflow-hidden relative group">
                                        <i class="far fa-envelope text-purple-600"></i>
                                        
                                        <span class="contact-text blur-md select-none transition-all duration-300 truncate max-w-[100px]">
                                            <%= closer.email %>
                                        </span>

                                        <div class="reveal-overlay absolute inset-0 flex items-center justify-center bg-white/90 group-hover:bg-white/60 backdrop-blur-[2px] transition-opacity duration-300">
                                            <span class="text-[9px] font-black opacity-0 group-hover:opacity-100 transition-opacity">RÉVÉLER</span>
                                        </div>
                                    </div>

                                    <div onclick="revealContact(this)" class="cursor-pointer border border-white/20 text-white py-3 rounded-xl font-bold text-center hover:bg-white/10 transition text-xs uppercase tracking-wide flex items-center justify-center gap-2 overflow-hidden relative group">
                                        <i class="fas fa-phone text-purple-400"></i>
                                        
                                        <span class="contact-text blur-md select-none transition-all duration-300">
                                            <%= closer.telephone %>
                                        </span>

                                        <div class="reveal-overlay absolute inset-0 flex items-center justify-center bg-black/80 group-hover:bg-black/40 backdrop-blur-[2px] transition-opacity duration-300">
                                            <span class="text-[9px] font-black opacity-0 group-hover:opacity-100 transition-opacity">VOIR</span>
                                        </div>
                                    </div>

                                <% } else { %>
                                    <a href="/register-choice" class="bg-white/10 text-gray-400 border border-white/5 py-3 rounded-xl font-bold text-center hover:bg-purple-600 hover:text-white hover:border-purple-500 transition text-xs uppercase tracking-wide flex items-center justify-center gap-2 group">
                                        <i class="fas fa-lock text-xs group-hover:animate-pulse"></i>
                                        <span>Email</span>
                                    </a>

                                    <a href="/register-choice" class="bg-white/10 text-gray-400 border border-white/5 py-3 rounded-xl font-bold text-center hover:bg-purple-600 hover:text-white hover:border-purple-500 transition text-xs uppercase tracking-wide flex items-center justify-center gap-2 group">
                                        <i class="fas fa-lock text-xs group-hover:animate-pulse"></i>
                                        <span>Tel</span>
                                    </a>
                                <% } %>

                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
        
        <% if (closers.length === 0) { %>
            <div class="text-center py-24 opacity-40">
                <i class="fas fa-search text-4xl mb-4"></i>
                <p class="text-xl font-light">Aucun talent ne correspond à ces critères.</p>
                <a href="/" class="text-purple-400 text-sm mt-2 hover:underline">Réinitialiser les filtres</a>
            </div>
        <% } %>

    </main>
<script>
    function revealContact(element) {
        // 1. On cible l'overlay (le cache)
        const overlay = element.querySelector('.reveal-overlay');
        
        // 2. On cible le texte
        const textSpan = element.querySelector('.contact-text');

        // 3. Si l'overlay existe encore, on fait la révélation
        if (overlay) {
            // On supprime TOTALEMENT l'overlay du code HTML (il ne reviendra plus)
            overlay.remove();
            
            // On enlève le flou et on rend le texte sélectionnable pour copier
            textSpan.classList.remove('blur-md', 'select-none');
            textSpan.classList.add('select-all'); // Permet de tout sélectionner en un clic
            textSpan.classList.remove('max-w-[100px]'); // On laisse la place au texte entier
        }
    }
</script>

</body>
</html>