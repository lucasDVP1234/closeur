<!DOCTYPE html>
<html lang="fr" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THE CLOSERS | Elite Directory</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;500;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { font-family: 'Space Grotesk', sans-serif; background-color: #050505; color: #ffffff; }
        
        .glass-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.08);
        }
        
        .neon-border:hover {
            border-color: rgba(147, 51, 234, 0.5);
            box-shadow: 0 0 25px rgba(147, 51, 234, 0.15);
        }

        /* Scrollbar invisible */
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }

        /* Animation Modal */
        .modal-enter { animation: modalIn 0.3s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
        @keyframes modalIn {
            from { opacity: 0; transform: scale(0.95) translateY(20px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }
    </style>
</head>
<body class="min-h-screen bg-[url('https://grainy-gradients.vercel.app/noise.svg')]">

    <nav class="border-b border-white/5 py-5 sticky top-0 z-50 bg-[#050505]/80 backdrop-blur-xl">
        <div class="max-w-7xl mx-auto px-6 flex justify-between items-center">
            <a href="/" class="group flex items-center gap-3">
                <div class="w-1 h-5 bg-purple-600 rounded-full shadow-[0_0_10px_rgba(147,51,234,0.8)] group-hover:h-8 group-hover:bg-white transition-all duration-300"></div>
                <h1 class="text-xl font-bold tracking-widest text-white">THE <span class="text-purple-500 group-hover:text-white transition-colors">CLOSERS</span>.</h1>
            </a>
            
            <div class="flex items-center gap-6">
                <% if (user) { %>
                    <div class="flex items-center gap-4 bg-white/5 border border-white/10 px-1 py-1 pr-4 rounded-full backdrop-blur-md">
                        <% if (user.role === 'closer') { %>
                            <a href="/dashboard" class="flex items-center gap-2 bg-white/10 hover:bg-purple-600 hover:text-white text-white px-4 py-2 rounded-full transition-all text-xs font-bold uppercase tracking-wide group">
                                <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse group-hover:bg-white"></span>
                                Cockpit
                            </a>
                        <% } else { %>
                            <div class="flex items-center gap-2 px-4 py-2">
                                <span class="text-lg">🏢</span>
                                <span class="text-sm font-bold text-gray-200"><%= user.name %></span>
                            </div>
                        <% } %>
                        <div class="w-px h-4 bg-white/10"></div>
                        <a href="/logout" class="text-red-400 hover:text-red-300 hover:bg-red-500/10 p-2 rounded-full transition-all"><i class="fas fa-power-off text-sm"></i></a>
                    </div>
                <% } else { %>
                    <div class="flex items-center gap-6">
                        <a href="/login" class="text-gray-400 hover:text-white text-sm font-bold transition-colors">Connexion</a>
                        <a href="/register-choice" class="bg-white text-black px-6 py-2.5 rounded-full font-bold hover:bg-purple-600 hover:text-white transition-all text-xs uppercase tracking-wider flex items-center gap-2">Rejoindre <span class="text-purple-600 group-hover:text-white">→</span></a>
                    </div>
                <% } %>
            </div>
        </div>
    </nav>

    <header class="pt-16 pb-10 px-6 max-w-7xl mx-auto">
        <h2 class="text-5xl md:text-7xl font-bold mb-8 text-transparent bg-clip-text bg-gradient-to-r from-white to-gray-600 tracking-tighter">
            L'élite du closing <br> à portée de main.
        </h2>
        
        <form action="/" method="GET" class="glass-card p-6 md:p-8 rounded-3xl mt-8 shadow-2xl relative overflow-hidden border border-purple-500/10">
            <div class="absolute top-0 right-0 w-96 h-96 bg-purple-900/10 rounded-full blur-3xl -mr-20 -mt-20 pointer-events-none"></div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 relative z-10">
                
                <div class="lg:col-span-2 space-y-2">
                    <label class="text-[9px] uppercase font-bold text-gray-500 tracking-wider ml-1">Recherche (Produit, Niche)</label>
                    <div class="relative">
                        <i class="fas fa-search absolute left-4 top-1/2 -translate-y-1/2 text-gray-500"></i>
                        <input type="text" name="product" placeholder="Ex: High Ticket, Immo, SaaS..." value="<%= filters.product || '' %>" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 pl-10 pr-4 text-white focus:border-purple-500 outline-none transition-all placeholder-gray-600">
                    </div>
                </div>

                <div class="space-y-2">
                    <label class="text-[9px] uppercase font-bold text-gray-500 tracking-wider ml-1">Type de Profil</label>
                    <select name="profileType" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 px-4 text-white focus:border-purple-500 outline-none appearance-none cursor-pointer">
                        <option value="">Tous les profils</option>
                        <option value="Closeur" <%= filters.profileType === 'Closeur' ? 'selected' : '' %>>Closeur</option>
                        <option value="Commercial" <%= filters.profileType === 'Commercial' ? 'selected' : '' %>>Commercial</option>
                        <option value="Setter" <%= filters.profileType === 'Setter' ? 'selected' : '' %>>Setter</option>
                    </select>
                </div>

                <div class="space-y-2">
                    <label class="text-[9px] uppercase font-bold text-gray-500 tracking-wider ml-1">Marché</label>
                    <select name="market" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 px-4 text-white focus:border-purple-500 outline-none appearance-none cursor-pointer">
                        <option value="">Tous marchés</option>
                        <option value="B2B" <%= filters.market === 'B2B' ? 'selected' : '' %>>B2B</option>
                        <option value="B2C" <%= filters.market === 'B2C' ? 'selected' : '' %>>B2C</option>
                        <option value="Les deux" <%= filters.market === 'Les deux' ? 'selected' : '' %>>Les deux</option>
                    </select>
                </div>

                <div class="space-y-2">
                    <label class="text-[9px] uppercase font-bold text-gray-500 tracking-wider ml-1">Expérience (Années min.)</label>
                    <input type="number" name="yearsExperience" placeholder="0" value="<%= filters.yearsExperience || '' %>" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 px-4 text-white focus:border-purple-500 outline-none">
                </div>

                <div class="space-y-2">
                    <label class="text-[9px] uppercase font-bold text-gray-500 tracking-wider ml-1">Type de Mission</label>
                    <select name="missionType" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 px-4 text-white focus:border-purple-500 outline-none appearance-none cursor-pointer">
                        <option value="">Peu importe</option>
                        <option value="Mission (Webi/Challenge)" <%= filters.missionType === 'Mission (Webi/Challenge)' ? 'selected' : '' %>>Webi / Challenge</option>
                        <option value="Long terme" <%= filters.missionType === 'Long terme' ? 'selected' : '' %>>Long terme</option>
                    </select>
                </div>

                <div class="space-y-2">
                    <label class="text-[9px] uppercase font-bold text-gray-500 tracking-wider ml-1">Total Closé (Min)</label>
                    <div class="relative">
                        <span class="absolute left-4 top-1/2 -translate-y-1/2 text-gray-500">€</span>
                        <input type="number" name="totalClosed" placeholder="Ex: 50000" value="<%= filters.totalClosed || '' %>" class="w-full bg-black/40 border border-white/10 rounded-xl py-3 pl-8 pr-4 text-white focus:border-purple-500 outline-none font-mono">
                    </div>
                </div>

                <div class="flex items-end">
                    <button type="submit" class="w-full bg-white text-black hover:bg-purple-600 hover:text-white py-3 rounded-xl font-bold transition-all shadow-[0_0_20px_rgba(255,255,255,0.1)] flex justify-center items-center gap-2 uppercase tracking-wide text-sm">
                        <i class="fas fa-filter text-xs"></i> Filtrer
                    </button>
                </div>
            </div>
        </form>
    </header>

    <main class="max-w-7xl mx-auto px-6 pb-20">
        
        <div class="flex justify-between items-center mb-6">
            <p class="text-gray-400 text-sm"><span class="text-white font-bold"><%= closers.length %></span> talents trouvés</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% closers.forEach(closer => { %>
                
                <div onclick="openModal('modal-<%= closer._id %>')" class="glass-card rounded-3xl overflow-hidden group neon-border transition-all duration-300 relative flex flex-col h-full cursor-pointer hover:-translate-y-2">
                    
                    <div class="h-64 w-full relative overflow-hidden">
                        <img src="<%= closer.photoUrl %>" alt="<%= closer.nom %>" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 grayscale group-hover:grayscale-0">
                        <div class="absolute bottom-0 left-0 w-full bg-gradient-to-t from-[#050505] to-transparent h-24"></div>
                        
                        <div class="absolute top-4 right-4 flex flex-col gap-2 items-end">
                            <span class="bg-black/80 backdrop-blur border border-white/20 px-3 py-1 text-[10px] font-bold uppercase tracking-wider rounded-full shadow-lg text-white">
                                <%= closer.yearsExperience %> Ans d'Exp.
                            </span>
                            <span class="bg-purple-600/90 backdrop-blur px-2 py-1 text-[9px] font-bold uppercase rounded-full shadow-lg text-white">
                                <%= closer.profileType %>
                            </span>
                        </div>
                    </div>

                    <div class="p-6 pt-2 flex flex-col flex-1 relative">
                        <div class="flex justify-between items-start mb-2">
                            <div>
                                <h3 class="text-2xl font-bold text-white leading-none mb-1"><%= closer.prenom %> <%= closer.nom.charAt(0) %>.</h3>
                                <p class="text-purple-400 text-xs font-mono uppercase"><%= closer.market %> • <%= closer.missionType %></p>
                            </div>
                        </div>

                        <div class="grid grid-cols-2 gap-3 mb-4">
                            <div class="bg-white/5 rounded-lg p-3 border border-white/5">
                                <p class="text-[9px] text-gray-500 uppercase tracking-wide mb-1">Total Closé</p>
                                <p class="text-white font-mono font-bold text-lg"><%= closer.totalClosed ? closer.totalClosed.toLocaleString() : '0' %>€</p>
                            </div>
                            <div class="bg-white/5 rounded-lg p-3 border border-white/5 overflow-hidden">
                                <p class="text-[9px] text-gray-500 uppercase tracking-wide mb-1">Expertise</p>
                                <div class="flex flex-wrap gap-1">
                                    <% if(closer.productTypes && closer.productTypes.length > 0) { %>
                                        <span class="text-[10px] text-gray-300 font-bold"><%= closer.productTypes[0] %></span>
                                        <% if(closer.productTypes.length > 1) { %>
                                            <span class="text-[10px] text-gray-500">+<%= closer.productTypes.length - 1 %></span>
                                        <% } %>
                                    <% } else { %>
                                        <span class="text-[9px] text-gray-500">-</span>
                                    <% } %>
                                </div>
                            </div>
                        </div>

                        <div class="flex flex-wrap gap-2 mb-6">
                            <% if(closer.contractType && closer.contractType.length > 0) { %>
                                <span class="px-2 py-1 bg-white/5 border border-white/10 rounded text-[9px] uppercase font-bold text-gray-300">
                                    <%= closer.contractType[0] %>
                                </span>
                            <% } %>
                            <span class="px-2 py-1 bg-white/5 border border-white/10 rounded text-[9px] uppercase font-bold text-gray-300">
                                <%= closer.availability %>
                            </span>
                        </div>

                        <div class="mt-auto pt-4 border-t border-white/5 flex justify-between items-center group-hover:opacity-100 transition-opacity">
                            <span class="text-xs text-gray-500 italic truncate max-w-[180px]">"<%= closer.closingPhilosophy ? closer.closingPhilosophy.substring(0, 25) + '...' : '...' %>"</span>
                            <div class="w-8 h-8 rounded-full bg-white/10 flex items-center justify-center group-hover:bg-purple-600 transition-colors">
                                <i class="fas fa-plus text-xs text-white"></i>
                            </div>
                        </div>
                    </div>
                </div>

                <div id="modal-<%= closer._id %>" class="fixed inset-0 z-[100] hidden items-center justify-center p-4">
                    <div class="absolute inset-0 bg-black/80 backdrop-blur-md" onclick="closeModal('modal-<%= closer._id %>')"></div>
                    
                    <div class="bg-[#0a0a0a] w-full max-w-4xl max-h-[90vh] overflow-y-auto rounded-3xl border border-white/10 shadow-2xl relative z-10 flex flex-col md:flex-row modal-enter">
                        
                        <button onclick="closeModal('modal-<%= closer._id %>')" class="absolute top-4 right-4 w-10 h-10 bg-black/50 rounded-full flex items-center justify-center text-white hover:bg-white hover:text-black transition z-20 border border-white/10">
                            <i class="fas fa-times"></i>
                        </button>

                        <div class="md:w-1/3 bg-white/5 p-8 border-r border-white/5 flex flex-col">
                            <div class="w-full aspect-square rounded-2xl overflow-hidden mb-6 border border-white/10 relative group">
                                <img src="<%= closer.photoUrl %>" class="w-full h-full object-cover">
                            </div>
                            
                            <h2 class="text-3xl font-bold mb-1"><%= closer.prenom %></h2>
                            <h2 class="text-3xl font-bold text-purple-500 mb-4"><%= closer.nom.charAt(0) %>.</h2>
                            
                            <div class="space-y-4 mb-8">
                                <div class="flex justify-between border-b border-white/10 pb-2">
                                    <span class="text-xs text-gray-500 uppercase">Expérience</span>
                                    <span class="text-sm font-bold"><%= closer.yearsExperience %> ans</span>
                                </div>
                                <div class="flex justify-between border-b border-white/10 pb-2">
                                    <span class="text-xs text-gray-500 uppercase">Total Closé</span>
                                    <span class="text-sm font-bold font-mono text-green-400"><%= closer.totalClosed ? closer.totalClosed.toLocaleString() : '0' %>€</span>
                                </div>
                                <div class="flex justify-between border-b border-white/10 pb-2">
                                    <span class="text-xs text-gray-500 uppercase">Mission</span>
                                    <span class="text-sm font-bold text-right"><%= closer.missionType %></span>
                                </div>
                                <div class="flex justify-between border-b border-white/10 pb-2">
                                    <span class="text-xs text-gray-500 uppercase">Dispo</span>
                                    <span class="text-sm font-bold"><%= closer.availability %></span>
                                </div>
                                <div class="flex justify-between border-b border-white/10 pb-2">
                                    <span class="text-xs text-gray-500 uppercase">Souhaité</span>
                                    <span class="text-sm font-bold"><%= closer.desiredIncome ? closer.desiredIncome + '€/m' : 'N/C' %></span>
                                </div>
                            </div>

                            <div class="mt-auto space-y-3">
                                <% if (user) { %>
                                    
                                    <div onclick="event.stopPropagation(); revealContact(this)" class="cursor-pointer bg-gray-100 border border-white/10 py-4 rounded-xl font-bold text-center hover:bg-white transition text-xs uppercase tracking-wide flex items-center justify-center gap-2 relative overflow-hidden group">
                                        <i class="far fa-envelope text-purple-600 z-10"></i>
                                        
                                        <span class="contact-text blur-md select-none transition-all duration-300 truncate max-w-[150px] text-gray-900 z-0">
                                            <%= closer.email %>
                                        </span>
                                        
                                        <div class="reveal-overlay absolute inset-0 flex items-center justify-center bg-gray-200/80 group-hover:bg-gray-100/50 backdrop-blur-[2px] transition-all z-20">
                                            <span class="text-[9px] font-black text-gray-800 uppercase tracking-widest">
                                                <i class="fas fa-eye mr-1"></i> Révéler Email
                                            </span>
                                        </div>
                                    </div>

                                    <div onclick="event.stopPropagation(); revealContact(this)" class="cursor-pointer bg-white/5 border border-white/20 py-4 rounded-xl font-bold text-center hover:bg-white/10 transition text-xs uppercase tracking-wide flex items-center justify-center gap-2 relative overflow-hidden group">
                                        <i class="fas fa-phone text-purple-400 z-10"></i>
                                        
                                        <span class="contact-text blur-md select-none transition-all duration-300 text-white z-0">
                                            <%= closer.telephone %>
                                        </span>
                                        
                                        <div class="reveal-overlay absolute inset-0 flex items-center justify-center bg-black/80 group-hover:bg-black/60 backdrop-blur-[2px] transition-all z-20">
                                            <span class="text-[9px] font-black text-gray-300 uppercase tracking-widest">
                                                <i class="fas fa-eye mr-1"></i> Révéler Tél
                                            </span>
                                        </div>
                                    </div>

                                <% } else { %>
                                    <a href="/register-choice" class="bg-white/10 text-gray-400 border border-white/5 py-4 rounded-xl font-bold text-center hover:bg-purple-600 hover:text-white transition flex items-center justify-center gap-2">
                                        <i class="fas fa-lock"></i> Voir Contact
                                    </a>
                                <% } %>
                            </div>
                        </div>

                        <div class="md:w-2/3 p-8">
                            
                            <div class="flex flex-wrap gap-2 mb-8">
                                <span class="bg-purple-600 px-3 py-1 rounded text-xs font-bold uppercase"><%= closer.profileType %></span>
                                <span class="bg-white/10 px-3 py-1 rounded text-xs uppercase"><%= closer.market %></span>
                                <% if(closer.productTypes) { %>
                                    <% closer.productTypes.forEach(type => { %>
                                        <span class="border border-white/20 px-3 py-1 rounded text-xs uppercase text-gray-300"><%= type %></span>
                                    <% }) %>
                                <% } %>
                            </div>

                            <div class="mb-8">
                                <h3 class="text-lg font-bold mb-4 flex items-center gap-2 text-purple-400">
                                    <i class="fas fa-brain"></i> Vision du Closing
                                </h3>
                                <div class="bg-white/5 p-6 rounded-2xl border-l-4 border-purple-500 italic text-gray-300 leading-relaxed relative">
                                    <i class="fas fa-quote-left absolute top-4 left-4 text-white/5 text-4xl -z-10"></i>
                                    "<%= closer.closingPhilosophy || 'Non renseigné.' %>"
                                </div>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                                <div>
                                    <h3 class="text-sm font-bold mb-3 uppercase text-gray-500"><i class="fas fa-file-contract mr-2"></i>Contrats acceptés</h3>
                                    <div class="flex flex-wrap gap-2">
                                        <% if(closer.contractType) { %>
                                            <% closer.contractType.forEach(c => { %>
                                                <span class="text-white text-xs bg-white/10 px-2 py-1 rounded"><%= c %></span>
                                            <% }) %>
                                        <% } %>
                                    </div>
                                </div>
                                <div>
                                    <h3 class="text-sm font-bold mb-3 uppercase text-gray-500"><i class="fas fa-users mr-2"></i>Anciens Clients</h3>
                                    <p class="text-white text-sm leading-relaxed"><%= closer.pastClients || 'Confidentiel / Non spécifié' %></p>
                                </div>
                            </div>

                            <% if (closer.videoUrl) { %>
                                <div>
                                    <h3 class="text-lg font-bold mb-4 flex items-center gap-2 text-purple-400">
                                        <i class="fas fa-video"></i> Démo / Présentation
                                    </h3>
                                    <a href="<%= closer.videoUrl %>" target="_blank" class="block w-full bg-black/40 border border-white/10 rounded-2xl p-6 hover:border-purple-500 hover:bg-purple-500/10 transition group flex items-center justify-center gap-4 relative overflow-hidden">
                                        <div class="absolute inset-0 bg-gradient-to-r from-purple-900/20 to-transparent opacity-0 group-hover:opacity-100 transition"></div>
                                        <div class="w-12 h-12 bg-white rounded-full flex items-center justify-center text-black group-hover:scale-110 transition-transform shadow-[0_0_15px_rgba(255,255,255,0.5)] z-10">
                                            <i class="fas fa-play ml-1"></i>
                                        </div>
                                        <div class="z-10">
                                            <p class="font-bold text-white">Regarder la vidéo</p>
                                            <p class="text-xs text-gray-500 truncate max-w-[200px]"><%= closer.videoUrl %></p>
                                        </div>
                                    </a>
                                </div>
                            <% } %>

                        </div>
                    </div>
                </div>

            <% }) %>
        </div>
        
        <% if (closers.length === 0) { %>
            <div class="text-center py-24 opacity-40">
                <i class="fas fa-ghost text-6xl mb-4 text-white/20"></i>
                <p class="text-xl font-light">Aucun talent trouvé.</p>
                <p class="text-sm text-gray-500 mt-2">Essayez d'ajuster vos filtres.</p>
                <a href="/" class="inline-block mt-4 text-purple-400 hover:text-white border border-purple-500/30 px-4 py-2 rounded-full text-xs uppercase tracking-wide transition">Réinitialiser</a>
            </div>
        <% } %>

    </main>

    <script>
        // Gestion des Modales
        function openModal(modalId) {
            const modal = document.getElementById(modalId);
            modal.classList.remove('hidden');
            modal.classList.add('flex');
            document.body.style.overflow = 'hidden'; 
        }

        function closeModal(modalId) {
            // Stop propagation
            if (event.target !== event.currentTarget && event.target.tagName !== 'BUTTON' && !event.target.classList.contains('fa-times')) return;

            const modal = document.getElementById(modalId);
            modal.classList.add('hidden');
            modal.classList.remove('flex');
            document.body.style.overflow = 'auto';
        }

        // Révélation Contact (Effet "Scratch card" digital)
        function revealContact(element) {
            const overlay = element.querySelector('.reveal-overlay');
            const textSpan = element.querySelector('.contact-text');

            if (overlay) {
                overlay.style.opacity = '0';
                setTimeout(() => overlay.remove(), 300); // Fade out
                
                textSpan.classList.remove('blur-md', 'select-none');
                textSpan.classList.add('select-all');
                textSpan.classList.remove('max-w-[150px]');
            }
        }
    </script>

</body>
</html>