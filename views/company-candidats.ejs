<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Candidats — <%= offer.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        display: ['Outfit', 'sans-serif'],
                        mono: ['JetBrains Mono', 'monospace'],
                    },
                    colors: {
                        surface: { 50: '#fafafa', 100: '#f4f4f5', 200: '#e4e4e7', 300: '#d4d4d8', 800: '#111113', 900: '#09090b', 950: '#050506' },
                        accent: { 400: '#a78bfa', 500: '#8b5cf6', 600: '#7c3aed', 700: '#6d28d9' },
                    }
                }
            }
        }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@200;300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <style>
        *, *::before, *::after { box-sizing: border-box; }
        body { font-family: 'Outfit', sans-serif; background: #050506; color: #fafafa; overflow-x: hidden; position: relative; }

        body::after {
            content: '';
            position: fixed; inset: -20%;
            z-index: 0;
            pointer-events: none;
            background:
                radial-gradient(900px 520px at 18% 12%, rgba(139, 92, 246, 0.16), transparent 60%),
                radial-gradient(820px 460px at 78% 18%, rgba(167, 139, 250, 0.12), transparent 62%),
                radial-gradient(720px 460px at 55% 78%, rgba(124, 58, 237, 0.10), transparent 65%);
            opacity: 0.9;
        }
        body > * { position: relative; z-index: 1; }

        body::before {
            content: '';
            position: fixed; inset: 0; z-index: 9999;
            pointer-events: none;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
            opacity: 0.5;
        }

        ::-webkit-scrollbar { width: 5px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: rgba(139,92,246,0.3); border-radius: 99px; }
        ::-webkit-scrollbar-thumb:hover { background: rgba(139,92,246,0.6); }
        ::selection { background: rgba(139,92,246,0.4); color: white; }

        .card-surface {
            background: linear-gradient(180deg, rgba(255,255,255,0.045), rgba(255,255,255,0.015));
            border: 1px solid rgba(255,255,255,0.07);
            box-shadow: 0 30px 80px -40px rgba(0,0,0,0.7), 0 0 0 1px rgba(255,255,255,0.02) inset;
            transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .card-surface:hover {
            border-color: rgba(139,92,246,0.2);
            box-shadow: 0 30px 80px -30px rgba(139,92,246,0.12), 0 0 0 1px rgba(139,92,246,0.08) inset;
            transform: translateY(-3px);
        }

        .card-premium {
            border-color: rgba(139,92,246,0.3) !important;
            box-shadow: 0 30px 80px -30px rgba(139,92,246,0.15), 0 0 0 1px rgba(139,92,246,0.1) inset !important;
        }
        .card-premium:hover {
            border-color: rgba(139,92,246,0.45) !important;
            box-shadow: 0 30px 80px -25px rgba(139,92,246,0.25), 0 0 0 1px rgba(139,92,246,0.15) inset !important;
        }

        .stat-box {
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.06);
        }

        .tag-chip {
            background: rgba(139,92,246,0.08);
            border: 1px solid rgba(139,92,246,0.15);
            color: rgba(167,139,250,0.9);
        }

        .contact-row {
            background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.06);
            transition: all 0.25s ease;
        }
        .contact-row:hover {
            background: rgba(139,92,246,0.06);
            border-color: rgba(139,92,246,0.15);
        }

        .header-surface {
            background: linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.01));
            border: 1px solid rgba(255,255,255,0.07);
            box-shadow: 0 20px 60px -30px rgba(0,0,0,0.6);
        }

        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(16px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-up { animation: fadeUp 0.5s ease-out forwards; }
    </style>
</head>
<body class="min-h-screen">

    <%- include('partials/nav', { user: (typeof user !== 'undefined' ? user : null) }) %>

    <div class="max-w-[1400px] mx-auto px-5 md:px-8 py-8 md:py-10">
        
        <!-- Header -->
        <div class="header-surface rounded-2xl p-6 md:p-8 mb-8 relative overflow-hidden animate-fade-up">
            <div class="absolute inset-0 opacity-30 pointer-events-none" style="background: radial-gradient(600px 300px at 10% 20%, rgba(139,92,246,0.15), transparent 60%);"></div>
            
            <div class="relative z-10 flex flex-col md:flex-row md:items-center justify-between gap-4">
                <div>
                    <a href="/company/dashboard" class="inline-flex items-center gap-2 text-[11px] font-semibold text-white/30 hover:text-accent-400 uppercase tracking-wider mb-3 transition-colors">
                        <i class="fas fa-arrow-left text-[9px]"></i> Retour dashboard
                    </a>
                    <h1 class="text-2xl md:text-3xl font-bold text-white tracking-tight">
                        Candidats pour <span class="text-white/50"><%= offer.title %></span>
                    </h1>
                    <div class="flex items-center gap-3 mt-2">
                        <% if (offer.niche) { %>
                            <span class="tag-chip text-[10px] font-semibold px-2.5 py-1 rounded-lg uppercase tracking-wider"><%= offer.niche %></span>
                        <% } %>
                        <% if (offer.missionType) { %>
                            <span class="text-[10px] font-medium text-white/25 uppercase tracking-wider"><%= offer.missionType %></span>
                        <% } %>
                    </div>
                </div>
                <div class="flex items-center gap-3">
                    <div class="px-5 py-3 rounded-xl stat-box text-center">
                        <span class="text-2xl font-bold font-mono text-white"><%= candidats.length %></span>
                        <p class="text-[9px] uppercase text-white/30 font-semibold tracking-wider mt-0.5">Candidat<%= candidats.length > 1 ? 's' : '' %></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Cards Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-5">
            
            <% if(candidats.length === 0) { %>
                <div class="col-span-full flex flex-col items-center justify-center py-24">
                    <div class="w-16 h-16 rounded-2xl bg-white/[0.03] border border-white/[0.06] flex items-center justify-center mb-4">
                        <i class="fas fa-user-clock text-white/15 text-xl"></i>
                    </div>
                    <p class="text-white/25 text-sm font-medium">Aucun candidat pour le moment</p>
                    <p class="text-white/15 text-xs mt-1">Les closers verront votre offre sur la page Missions</p>
                </div>
            <% } %>

            <% candidats.forEach((candidat, index) => { %>
                <div class="card-surface <%= candidat.isPremium ? 'card-premium' : '' %> rounded-2xl overflow-hidden" style="animation: fadeUp 0.5s ease-out forwards; animation-delay: <%= index * 0.08 %>s; opacity: 0;">
                    
                    <!-- Top section: Identity -->
                    <div class="p-5 pb-4">
                        <div class="flex items-start gap-4">
                            <!-- Photo -->
                            <div class="relative shrink-0">
                                <div class="w-16 h-16 rounded-xl overflow-hidden border border-white/10 <%= candidat.isPremium ? 'border-accent-500/30' : '' %>">
                                    <img src="<%= candidat.photoUrl || 'https://via.placeholder.com/150' %>" class="w-full h-full object-cover" alt="<%= candidat.prenom %>">
                                </div>
                                <% if(candidat.isPremium) { %>
                                    <div class="absolute -top-1.5 -right-1.5 w-6 h-6 rounded-lg bg-gradient-to-br from-accent-500 to-accent-700 flex items-center justify-center shadow-lg shadow-accent-500/30">
                                        <i class="far fa-gem text-white text-[8px]"></i>
                                    </div>
                                <% } %>
                            </div>
                            
                            <!-- Info -->
                            <div class="flex-1 min-w-0">
                                <div class="flex items-center gap-2 flex-wrap">
                                    <h3 class="font-bold text-lg text-white leading-tight"><%= candidat.prenom %> <%= candidat.nom.charAt(0) %>.</h3>
                                    <% if(candidat.isPremium) { %>
                                        <span class="text-[9px] font-bold uppercase tracking-widest text-accent-400 bg-accent-500/10 px-2 py-0.5 rounded-md border border-accent-500/15">Pro</span>
                                    <% } %>
                                </div>
                                <p class="text-xs text-white/40 font-medium mt-0.5"><%= candidat.profileType %></p>
                                
                                <div class="flex items-center gap-3 mt-2 flex-wrap">
                                    <% if(candidat.availability) { %>
                                        <span class="inline-flex items-center gap-1.5 text-[10px] text-white/50">
                                            <span class="w-1.5 h-1.5 rounded-full <%= candidat.availability === 'Full Time' ? 'bg-emerald-400' : 'bg-amber-400' %>"></span>
                                            <%= candidat.availability %>
                                        </span>
                                    <% } %>
                                    <% if(candidat.market) { %>
                                        <span class="text-[10px] text-white/35 font-mono"><%= candidat.market %></span>
                                    <% } %>
                                    <% if(candidat.missionType) { %>
                                        <span class="text-[10px] text-white/35"><%= candidat.missionType %></span>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Stats row -->
                    <div class="grid grid-cols-3 mx-5 mb-4 rounded-xl overflow-hidden stat-box divide-x divide-white/[0.05]">
                        <div class="py-3 px-3 text-center">
                            <p class="text-[9px] text-white/25 uppercase tracking-wider font-semibold mb-1">CA Closé</p>
                            <p class="font-mono font-bold text-sm text-white"><%= candidat.totalClosed ? candidat.totalClosed.toLocaleString('fr-FR') + '€' : '—' %></p>
                        </div>
                        <div class="py-3 px-3 text-center">
                            <p class="text-[9px] text-white/25 uppercase tracking-wider font-semibold mb-1">Expérience</p>
                            <p class="font-mono font-bold text-sm text-white"><%= candidat.yearsExperience ? candidat.yearsExperience + ' ans' : '—' %></p>
                        </div>
                        <div class="py-3 px-3 text-center">
                            <p class="text-[9px] text-white/25 uppercase tracking-wider font-semibold mb-1">Rémun. visée</p>
                            <p class="font-mono font-bold text-sm text-white"><%= candidat.desiredIncome ? candidat.desiredIncome.toLocaleString('fr-FR') + '€' : '—' %></p>
                        </div>
                    </div>

                    <!-- Product types -->
                    <% if(candidat.productTypes && candidat.productTypes.length > 0) { %>
                        <div class="px-5 mb-4">
                            <p class="text-[9px] text-white/20 uppercase tracking-wider font-semibold mb-2">Niches maîtrisées</p>
                            <div class="flex flex-wrap gap-1.5">
                                <% candidat.productTypes.forEach(type => { %>
                                    <span class="tag-chip text-[10px] font-medium px-2.5 py-1 rounded-lg"><%= type.replace(/_/g, ' ') %></span>
                                <% }) %>
                            </div>
                        </div>
                    <% } %>

                    <!-- Contract type -->
                    <% if(candidat.contractType && candidat.contractType.length > 0) { %>
                        <div class="px-5 mb-4">
                            <p class="text-[9px] text-white/20 uppercase tracking-wider font-semibold mb-2">Contrat recherché</p>
                            <div class="flex flex-wrap gap-1.5">
                                <% candidat.contractType.forEach(ct => { %>
                                    <span class="text-[10px] font-medium px-2.5 py-1 rounded-lg bg-white/[0.04] border border-white/[0.08] text-white/50"><%= ct %></span>
                                <% }) %>
                            </div>
                        </div>
                    <% } %>

                    <!-- Past clients -->
                    <% if(candidat.pastClients) { %>
                        <div class="px-5 mb-4">
                            <p class="text-[9px] text-white/20 uppercase tracking-wider font-semibold mb-1.5">Partenaires passés</p>
                            <p class="text-xs text-white/45 leading-relaxed"><%= candidat.pastClients %></p>
                        </div>
                    <% } %>

                    <!-- Philosophy -->
                    <% if(candidat.closingPhilosophy) { %>
                        <div class="px-5 mb-4">
                            <p class="text-[9px] text-white/20 uppercase tracking-wider font-semibold mb-1.5">Philosophie</p>
                            <p class="text-xs text-white/40 leading-relaxed italic">"<%= candidat.closingPhilosophy %>"</p>
                        </div>
                    <% } %>

                    <!-- Separator -->
                    <div class="mx-5 border-t border-white/[0.05]"></div>

                    <!-- Contact section -->
                    <div class="p-5 space-y-2">
                        <p class="text-[9px] text-white/20 uppercase tracking-wider font-semibold mb-2.5">Contact direct</p>
                        
                        <a href="mailto:<%= candidat.email %>" class="contact-row flex items-center gap-3 p-3 rounded-xl group/link">
                            <div class="w-8 h-8 rounded-lg bg-accent-500/8 border border-accent-500/10 flex items-center justify-center shrink-0">
                                <i class="fas fa-envelope text-accent-400/60 group-hover/link:text-accent-400 text-[10px] transition-colors"></i>
                            </div>
                            <span class="text-xs font-mono text-white/60 group-hover/link:text-white/90 transition-colors truncate"><%= candidat.email %></span>
                            <i class="fas fa-arrow-right text-[8px] text-white/15 group-hover/link:text-accent-400 ml-auto transition-colors shrink-0"></i>
                        </a>

                        <a href="tel:<%= candidat.telephone %>" class="contact-row flex items-center gap-3 p-3 rounded-xl group/link">
                            <div class="w-8 h-8 rounded-lg bg-accent-500/8 border border-accent-500/10 flex items-center justify-center shrink-0">
                                <i class="fas fa-phone text-accent-400/60 group-hover/link:text-accent-400 text-[10px] transition-colors"></i>
                            </div>
                            <span class="text-xs font-mono text-white/60 group-hover/link:text-white/90 transition-colors"><%= candidat.telephone %></span>
                            <i class="fas fa-arrow-right text-[8px] text-white/15 group-hover/link:text-accent-400 ml-auto transition-colors shrink-0"></i>
                        </a>

                        <% if(candidat.videoUrl) { %>
                            <a href="<%= candidat.videoUrl %>" target="_blank" class="contact-row flex items-center gap-3 p-3 rounded-xl group/link border-accent-500/10">
                                <div class="w-8 h-8 rounded-lg bg-red-500/8 border border-red-500/10 flex items-center justify-center shrink-0">
                                    <i class="fas fa-play text-red-400/60 group-hover/link:text-red-400 text-[10px] transition-colors"></i>
                                </div>
                                <span class="text-xs font-medium text-white/50 group-hover/link:text-white/80 transition-colors">Voir la vidéo de présentation</span>
                                <i class="fas fa-external-link-alt text-[8px] text-white/15 group-hover/link:text-accent-400 ml-auto transition-colors shrink-0"></i>
                            </a>
                        <% } %>
                    </div>

                    <!-- Footer with date -->
                    <div class="px-5 py-3 border-t border-white/[0.04] flex items-center justify-between">
                        <span class="text-[10px] text-white/15 font-medium">
                            Membre depuis <%= candidat.createdAt ? new Date(candidat.createdAt).toLocaleDateString('fr-FR', { month: 'short', year: 'numeric' }) : '—' %>
                        </span>
                        <% if(candidat.isPremium) { %>
                            <span class="text-[9px] text-accent-400/50 font-medium uppercase tracking-wider">Closer Vérifié</span>
                        <% } %>
                    </div>

                </div>
            <% }) %>
        </div>
    </div>

</body>
</html>