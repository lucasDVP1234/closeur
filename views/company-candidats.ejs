<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Candidats — <%= offer.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        display: ['Outfit', 'sans-serif'],
                        mono: ['JetBrains Mono', 'monospace'],
                    },
                    colors: {
                        surface: { 50: '#fafafa', 100: '#f4f4f5', 200: '#e4e4e7', 300: '#d4d4d8', 800: '#111113', 900: '#09090b', 950: '#050506' },
                        accent: { 400: '#a78bfa', 500: '#8b5cf6', 600: '#7c3aed', 700: '#6d28d9' },
                        gold: { 300: '#fcd34d', 400: '#fbbf24', 500: '#f59e0b', 600: '#d97706' }
                    }
                }
            }
        }
    </script>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@200;300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <style>body { font-family: 'Outfit', sans-serif; background: #050506; color: #fff; }</style>
</head>
<body class="min-h-screen bg-[#050506]">

    <%- include('partials/nav', { user: (typeof user !== 'undefined' ? user : null) }) %>

    <div class="max-w-[1400px] mx-auto px-5 md:px-8 py-10">
        
        <div class="flex items-center justify-between mb-10 border-b border-white/10 pb-6">
            <div>
                <a href="/company/dashboard" class="text-xs font-bold text-white/40 hover:text-white uppercase tracking-widest mb-2 inline-block"><i class="fas fa-arrow-left mr-2"></i> Retour dashboard</a>
                <h1 class="text-3xl font-black text-white">Candidats pour : <span class="text-white/60"><%= offer.title %></span></h1>
            </div>
            <div class="px-4 py-2 bg-white/5 rounded-lg border border-white/10">
                <span class="text-xl font-bold font-mono"><%= candidats.length %></span> <span class="text-xs uppercase text-white/40 font-bold ml-1">Postulants</span>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            
            <% if(candidats.length === 0) { %>
                <div class="col-span-full text-center py-20 text-white/30 italic">
                    Aucun candidat n'a postulé pour le moment.
                </div>
            <% } %>

            <% candidats.forEach(candidat => { %>
                <div class="bg-[#09090b] border border-white/10 rounded-2xl overflow-hidden group hover:border-white/30 transition-all duration-300">
                    
                    <div class="p-6 border-b border-white/5 bg-gradient-to-b from-white/[0.02] to-transparent">
                        <div class="flex items-start justify-between gap-4">
                            <div class="flex items-center gap-4">
                                <div class="w-14 h-14 rounded-xl overflow-hidden border border-white/10">
                                    <img src="<%= candidat.photoUrl || 'https://via.placeholder.com/150' %>" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all">
                                </div>
                                <div>
                                    <h3 class="font-bold text-lg text-white"><%= candidat.prenom %> <%= candidat.nom.charAt(0) %>.</h3>
                                    <p class="text-xs text-white/40 uppercase tracking-widest"><%= candidat.profileType %></p>
                                    
                                    <% if(candidat.isPremium) { %>
                                        <div class="mt-1 inline-flex items-center gap-1.5 px-2 py-0.5 rounded border border-white/20 bg-white/5">
                                            <i class="far fa-gem text-[9px] text-white"></i>
                                            <span class="text-[9px] font-bold uppercase text-white tracking-widest">Élite</span>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 border-b border-white/5 divide-x divide-white/5 bg-black/20">
                        <div class="p-3 text-center">
                            <p class="text-[9px] text-white/30 uppercase tracking-widest mb-1">Closé</p>
                            <p class="font-mono font-bold text-sm"><%= candidat.totalClosed %>€</p>
                        </div>
                        <div class="p-3 text-center">
                            <p class="text-[9px] text-white/30 uppercase tracking-widest mb-1">Expérience</p>
                            <p class="font-mono font-bold text-sm"><%= candidat.yearsExperience %> ans</p>
                        </div>
                    </div>

                    <div class="p-5 space-y-3 bg-[#0a0a0c]">
                        <p class="text-[10px] font-bold uppercase text-white/20 mb-2">Coordonnées Directes</p>
                        
                        <a href="mailto:<%= candidat.email %>" class="flex items-center gap-3 p-3 rounded-lg bg-white/5 hover:bg-white/10 transition-colors group/link">
                            <div class="w-8 h-8 rounded bg-black flex items-center justify-center text-white/50 group-hover/link:text-white">
                                <i class="fas fa-envelope text-xs"></i>
                            </div>
                            <span class="text-xs font-mono text-white/80"><%= candidat.email %></span>
                        </a>

                        <a href="tel:<%= candidat.telephone %>" class="flex items-center gap-3 p-3 rounded-lg bg-white/5 hover:bg-white/10 transition-colors group/link">
                            <div class="w-8 h-8 rounded bg-black flex items-center justify-center text-white/50 group-hover/link:text-white">
                                <i class="fas fa-phone text-xs"></i>
                            </div>
                            <span class="text-xs font-mono text-white/80"><%= candidat.telephone %></span>
                        </a>

                        <% if(candidat.videoUrl) { %>
                            <a href="<%= candidat.videoUrl %>" target="_blank" class="flex items-center gap-3 p-3 rounded-lg border border-white/10 hover:border-white/30 transition-colors group/link mt-2">
                                <div class="w-8 h-8 rounded bg-black flex items-center justify-center text-red-500/80">
                                    <i class="fas fa-play text-xs"></i>
                                </div>
                                <span class="text-xs font-bold uppercase tracking-wide text-white/60 group-hover/link:text-white">Voir vidéo de présentation</span>
                            </a>
                        <% } %>
                    </div>

                </div>
            <% }) %>
        </div>
    </div>

</body>
</html>