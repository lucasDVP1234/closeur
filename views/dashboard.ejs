<!DOCTYPE html>
<html lang="fr" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cockpit - THE CLOSERS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;500;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Space Grotesk', sans-serif; background-color: #050505; color: #fff; }
        .glass-panel {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.08);
        }
        .neon-text {
            text-shadow: 0 0 10px rgba(168, 85, 247, 0.5);
        }
    </style>
</head>
<body class="min-h-screen bg-[url('https://grainy-gradients.vercel.app/noise.svg')] pb-20">

    <nav class="border-b border-white/5 py-4 bg-[#050505]/90 backdrop-blur-md sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6 flex justify-between items-center">
            <div class="flex items-center gap-3">
                <div class="w-2 h-8 bg-purple-600 rounded-full shadow-[0_0_15px_rgba(147,51,234,0.8)]"></div>
                <h1 class="text-xl font-bold tracking-widest text-white">THE <span class="text-purple-500">CLOSERS</span> <span class="text-xs text-gray-600 bg-white/5 px-2 py-1 rounded ml-2 border border-white/5">COCKPIT</span></h1>
            </div>
            
            <div class="flex items-center gap-6">
                <a href="/" class="text-gray-400 hover:text-white text-sm transition flex items-center gap-2">
                    <i class="fas fa-globe"></i> Voir l'annuaire
                </a>
                <div class="h-6 w-px bg-white/10"></div>
                <a href="/logout" class="text-xs text-red-400 border border-red-500/20 px-4 py-2 rounded-full hover:bg-red-500/10 transition">
                    <i class="fas fa-power-off mr-1"></i> DÉCONNEXION
                </a>
            </div>
        </div>
    </nav>

    <div class="max-w-7xl mx-auto px-6 mt-10">
        
        <div class="mb-10">
            <h2 class="text-4xl font-bold mb-2">Bon retour, <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-indigo-400"><%= closer.prenom %></span>.</h2>
            <p class="text-gray-500">Voici vos performances actuelles sur le marché.</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
            <div class="glass-panel p-6 rounded-2xl relative overflow-hidden group">
                <div class="absolute top-0 right-0 w-24 h-24 bg-purple-600/10 rounded-full blur-2xl -mr-10 -mt-10 group-hover:bg-purple-600/20 transition-all"></div>
                <div class="flex justify-between items-start mb-4">
                    <div class="bg-purple-500/20 p-3 rounded-lg text-purple-400"><i class="fas fa-euro-sign text-xl"></i></div>
                    <span class="text-xs font-mono text-green-400 bg-green-400/10 px-2 py-1 rounded">+12% vs last month</span>
                </div>
                <h3 class="text-gray-400 text-xs uppercase tracking-wider mb-1">Total Closé</h3>
                <p class="text-3xl font-bold font-mono"><%= closer.totalClosed.toLocaleString() %> €</p>
            </div>

            <div class="glass-panel p-6 rounded-2xl relative overflow-hidden group">
                <div class="absolute top-0 right-0 w-24 h-24 bg-blue-600/10 rounded-full blur-2xl -mr-10 -mt-10 group-hover:bg-blue-600/20 transition-all"></div>
                <div class="flex justify-between items-start mb-4">
                    <div class="bg-blue-500/20 p-3 rounded-lg text-blue-400"><i class="fas fa-trophy text-xl"></i></div>
                    <span class="text-xs font-mono text-gray-400">Niveau Actuel</span>
                </div>
                <h3 class="text-gray-400 text-xs uppercase tracking-wider mb-1">Grade</h3>
                <p class="text-3xl font-bold text-white"><%= closer.experienceLevel %></p>
            </div>

            <div class="glass-panel p-6 rounded-2xl relative overflow-hidden group">
                <div class="absolute top-0 right-0 w-24 h-24 bg-pink-600/10 rounded-full blur-2xl -mr-10 -mt-10 group-hover:bg-pink-600/20 transition-all"></div>
                <div class="flex justify-between items-start mb-4">
                    <div class="bg-pink-500/20 p-3 rounded-lg text-pink-400"><i class="fas fa-eye text-xl"></i></div>
                    <span class="text-xs font-mono text-gray-400">Cette semaine</span>
                </div>
                <h3 class="text-gray-400 text-xs uppercase tracking-wider mb-1">Vues Profil</h3>
                <p class="text-3xl font-bold">142 <span class="text-sm text-gray-500 font-normal">entreprises</span></p>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            
            <div class="lg:col-span-1 space-y-6">
                <h3 class="text-sm font-bold text-gray-500 uppercase tracking-widest mb-4">Votre Carte Publique</h3>
                
                <div class="glass-panel rounded-3xl overflow-hidden border-2 border-white/5 relative group">
                    <div class="h-80 w-full relative">
                        <img src="<%= closer.photoUrl %>" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-700">
                        <div class="absolute bottom-0 left-0 w-full h-32 bg-gradient-to-t from-black to-transparent"></div>
                        <div class="absolute bottom-4 left-4">
                            <h2 class="text-2xl font-bold"><%= closer.prenom %> <%= closer.nom %></h2>
                            <p class="text-purple-400 font-mono text-sm"><%= closer.experienceLevel %></p>
                        </div>
                        <div class="absolute top-4 right-4 bg-black/60 backdrop-blur px-3 py-1 rounded-full border border-white/10 flex items-center gap-2">
                            <div class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
                            <span class="text-[10px] font-bold uppercase">Dispo</span>
                        </div>
                    </div>
                    
                    <div class="p-6 space-y-4">
                        <div class="flex flex-wrap gap-2">
                            <% closer.skills.forEach(skill => { %>
                                <span class="text-[10px] uppercase px-2 py-1 bg-white/10 rounded border border-white/5"><%= skill %></span>
                            <% }) %>
                        </div>
                        <div class="pt-4 border-t border-white/10 flex justify-between items-center text-sm">
                            <span class="text-gray-500">Dernier Challenge</span>
                            <span class="text-white font-medium truncate max-w-[150px]"><%= closer.lastChallenge %></span>
                        </div>
                    </div>
                </div>

                <div class="glass-panel p-6 rounded-2xl border border-yellow-500/20 bg-yellow-500/5">
                    <div class="flex items-center gap-4 mb-2">
                        <i class="fas fa-lightbulb text-yellow-500"></i>
                        <h4 class="font-bold text-yellow-500">Conseil Pro</h4>
                    </div>
                    <p class="text-xs text-gray-400 leading-relaxed">
                        Les profils avec une photo professionnelle et plus de 3 "Skills" précis ont 40% plus de chances d'être contactés.
                    </p>
                </div>
            </div>

            <div class="lg:col-span-2">
                <div class="glass-panel rounded-3xl p-8 md:p-10 relative overflow-hidden">
                    <div class="absolute inset-0 bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-50 pointer-events-none"></div>

                    <div class="relative z-10">
                        <div class="flex justify-between items-center mb-8 border-b border-white/10 pb-6">
                            <div>
                                <h3 class="text-2xl font-bold">Configuration du Profil</h3>
                                <p class="text-sm text-gray-500">Mettez à jour vos données en temps réel.</p>
                            </div>
                            <button form="updateForm" class="bg-white text-black hover:bg-purple-500 hover:text-white px-8 py-3 rounded-xl font-bold transition-all shadow-lg flex items-center gap-2">
                                <i class="fas fa-save"></i> SAUVEGARDER
                            </button>
                        </div>

                        <form id="updateForm" action="/dashboard/update" method="POST" enctype="multipart/form-data" class="space-y-8">
                            
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                                <div class="col-span-1">
                                    <label class="block text-xs uppercase text-gray-500 font-bold mb-3 tracking-wider">Photo de profil</label>
                                    <div class="relative w-full aspect-square bg-black/50 border-2 border-dashed border-white/20 rounded-2xl flex flex-col items-center justify-center hover:border-purple-500 hover:bg-purple-500/10 transition cursor-pointer group overflow-hidden">
                                        <input type="file" name="photo" class="absolute inset-0 opacity-0 cursor-pointer z-20">
                                        <img src="<%= closer.photoUrl %>" class="absolute inset-0 w-full h-full object-cover opacity-50 group-hover:opacity-30 transition">
                                        <i class="fas fa-camera text-3xl mb-2 text-white z-10"></i>
                                        <span class="text-xs text-gray-400 z-10 group-hover:text-white">Modifier</span>
                                    </div>
                                </div>
                                <div class="col-span-2 space-y-6">
                                    <div class="grid grid-cols-2 gap-6">
                                        <div>
                                            <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Prénom</label>
                                            <input type="text" name="prenom" value="<%= closer.prenom %>" class="w-full bg-black/40 border border-white/10 rounded-xl p-4 text-white focus:border-purple-500 outline-none transition-all">
                                        </div>
                                        <div>
                                            <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Nom</label>
                                            <input type="text" name="nom" value="<%= closer.nom %>" class="w-full bg-black/40 border border-white/10 rounded-xl p-4 text-white focus:border-purple-500 outline-none transition-all">
                                        </div>
                                    </div>
                                    <div>
                                        <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Bio / Punchline</label>
                                        <textarea name="bio" rows="2" placeholder="Votre phrase d'accroche..." class="w-full bg-black/40 border border-white/10 rounded-xl p-4 text-white focus:border-purple-500 outline-none transition-all"><%= closer.bio %></textarea>
                                    </div>
                                </div>
                            </div>

                            <hr class="border-white/5">

                            <div>
                                <h4 class="text-lg font-bold mb-6 flex items-center gap-2"><i class="fas fa-chart-line text-purple-500"></i> Données Performance</h4>
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <div>
                                        <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Total Closé (Lifetime)</label>
                                        <div class="relative">
                                            <span class="absolute left-4 top-1/2 -translate-y-1/2 text-gray-500">€</span>
                                            <input type="number" name="totalClosed" value="<%= closer.totalClosed %>" class="w-full bg-black/40 border border-white/10 rounded-xl p-4 pl-8 text-white focus:border-purple-500 outline-none font-mono text-lg tracking-wider">
                                        </div>
                                    </div>
                                    <div>
                                        <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Niveau d'Expérience</label>
                                        <div class="relative">
                                            <select name="experienceLevel" class="w-full bg-black/40 border border-white/10 rounded-xl p-4 text-white focus:border-purple-500 outline-none appearance-none cursor-pointer">
                                                <option value="Junior" <%= closer.experienceLevel === 'Junior' ? 'selected' : '' %>>Junior (0 - 50k)</option>
                                                <option value="Confirmé" <%= closer.experienceLevel === 'Confirmé' ? 'selected' : '' %>>Confirmé (50k - 200k)</option>
                                                <option value="Expert" <%= closer.experienceLevel === 'Expert' ? 'selected' : '' %>>Expert (200k - 500k)</option>
                                                <option value="Killer" <%= closer.experienceLevel === 'Killer' ? 'selected' : '' %>>Killer (500k+)</option>
                                            </select>
                                            <i class="fas fa-chevron-down absolute right-4 top-1/2 -translate-y-1/2 text-gray-500 pointer-events-none"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div>
                                <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Compétences (Tags)</label>
                                <input type="text" name="skills" value="<%= closer.skills.join(', ') %>" class="w-full bg-black/40 border border-white/10 rounded-xl p-4 text-white focus:border-purple-500 outline-none transition-all placeholder-gray-600">
                                <p class="text-[10px] text-gray-500 mt-2 ml-1">Séparez par des virgules (Ex: High Ticket, Immo, B2B)</p>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                <div>
                                    <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Dernier Gros Challenge</label>
                                    <input type="text" name="lastChallenge" value="<%= closer.lastChallenge %>" class="w-full bg-black/40 border border-white/10 rounded-xl p-4 text-white focus:border-purple-500 outline-none">
                                </div>
                                <div>
                                    <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Awards / Prix / Trophées</label>
                                    <input type="text" name="awards" value="<%= closer.awards %>" class="w-full bg-black/40 border border-white/10 rounded-xl p-4 text-white focus:border-purple-500 outline-none">
                                </div>
                            </div>
                            
                            <div>
                                <label class="block text-xs uppercase text-gray-500 font-bold mb-2 ml-1">Zone Fun (Vaisselle, Hobby...)</label>
                                <input type="text" name="misc" value="<%= closer.misc %>" class="w-full bg-black/40 border border-white/10 rounded-xl p-4 text-white focus:border-purple-500 outline-none italic">
                            </div>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>
</html>