<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Opportunités — THE CLOSERS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;700;900&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <style>body { font-family: 'Outfit', sans-serif; background: #050506; color: #fff; }</style>
</head>
<body class="min-h-screen pb-20">

    <nav class="border-b border-white/10 bg-black/20 backdrop-blur-md sticky top-0 z-50">
        <div class="max-w-5xl mx-auto px-6 h-16 flex items-center justify-between">
            <a href="/" class="font-black tracking-widest text-sm">SEFAIRECLOSER.COM</a>
            <div class="flex gap-4 text-xs font-bold uppercase tracking-wide">
                <a href="/" class="opacity-50 hover:opacity-100 transition">Annuaire</a>
                <% if(user.role === 'company') { %>
                    <a href="/company/dashboard" class="text-white">Mon Espace Recruteur</a>
                <% } else { %>
                    <a href="/dashboard" class="opacity-50 hover:opacity-100 transition">Mon Profil</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="max-w-5xl mx-auto px-6 py-12">
        <header class="mb-12 text-center">
            <h1 class="text-4xl md:text-5xl font-black tracking-tight mb-4">Opportunités <span class="text-transparent bg-clip-text bg-gradient-to-br from-white to-slate-500">Exclusives</span></h1>
            <p class="text-white/40 max-w-xl mx-auto">Accédez aux missions postées par les entreprises vérifiées. Postulez en un clic.</p>
        </header>

        <div class="grid gap-6">
            <% if(offers.length === 0) { %>
                <div class="text-center py-20 border border-dashed border-white/10 rounded-2xl">
                    <p class="text-white/30">Aucune offre disponible pour le moment.</p>
                </div>
            <% } %>

            <% offers.forEach(offer => { %>
                <div class="group bg-[#09090b] border border-white/10 p-6 rounded-2xl hover:border-white/30 transition-all duration-300 relative overflow-hidden">
                    <div class="flex flex-col md:flex-row md:items-start justify-between gap-6">
                        
                        <div class="flex-1">
                            <div class="flex items-center gap-3 mb-2">
                                <span class="text-[10px] font-bold uppercase tracking-widest px-2 py-1 rounded bg-white/5 text-white/60"><%= offer.missionType %></span>
                                <span class="text-[10px] font-bold uppercase tracking-widest px-2 py-1 rounded bg-emerald-500/10 text-emerald-400"><%= offer.remuneration %></span>
                            </div>
                            <h3 class="text-xl font-bold text-white mb-1"><%= offer.title %></h3>
                            <p class="text-sm text-white/40 mb-4 font-mono uppercase tracking-wide">Par <%= offer.companyName %></p>
                            
                            <p class="text-white/70 text-sm leading-relaxed max-w-2xl"><%= offer.description %></p>
                        </div>

                        <div class="flex items-center gap-4 shrink-0">
                            <% if (user.role === 'closer') { %>
                                <% if (offer.applicants && offer.applicants.includes(user.id)) { %>
                                    <button disabled class="px-6 py-3 rounded-xl bg-green-500/20 text-green-400 font-bold text-xs uppercase tracking-widest border border-green-500/20 cursor-default">
                                        <i class="fas fa-check mr-2"></i> Candidature envoyée
                                    </button>
                                <% } else { %>
                                    <form action="/offres/apply/<%= offer._id %>" method="POST">
                                        <button type="submit" class="px-6 py-3 rounded-xl bg-white text-black font-black text-xs uppercase tracking-widest hover:scale-105 transition-transform shadow-[0_0_20px_rgba(255,255,255,0.2)]">
                                            Postuler
                                        </button>
                                    </form>
                                <% } %>
                            <% } else { %>
                                <span class="text-xs text-white/30 italic">Connectez-vous en tant que Closer</span>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>